# Port targets — `arc-swap-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `arc-swap` that codex-rs uses, and therefore the minimum subset that `arc-swap-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-12] (rank `156`, tier `direct`, status `To Do`)

**Cargo package name**: `arc-swap`
**Rust module name (in `use` statements)**: `arc_swap`

## Summary

- **1** distinct symbols imported by codex-rs
- **3** total `use` statements
- **3** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `arc_swap::ArcSwap`

- **3 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `codex-exec-server`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/exec_policy.rs:5` (`codex-core`) — `use arc_swap::ArcSwap`
- `core/src/state/service.rs:17` (`codex-core`) — `use arc_swap::ArcSwap`
- `exec-server/src/client.rs:8` (`codex-exec-server`) — `use arc_swap::ArcSwap`
